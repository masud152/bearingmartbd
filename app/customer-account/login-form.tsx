"use client";
import { useState,type FormEvent } from "react";
export default function CustomerLoginForm(){const [busy,setBusy]=useState(false),[message,setMessage]=useState("");async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setMessage("");const form=new FormData(event.currentTarget);const response=await fetch("/api/customers/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({identifier:form.get("identifier"),password:form.get("password")})});const data=await response.json() as {error?:string};setBusy(false);if(!response.ok){setMessage(data.error??"Unable to sign in.");return;}window.location.assign("/products/ball-bearings/catalog")}return <form className="customer-login-form" onSubmit={submit}><label>Email address or mobile number<input name="identifier" required autoComplete="username" placeholder="Email or mobile number"/></label><label>Password<input name="password" type="password" required autoComplete="current-password"/></label><a className="forgot-password-link" href="mailto:contact@bearingmartbd.com?subject=Password%20reset%20request%20-%20Bearing%20Mart%20BD&body=Please%20help%20me%20reset%20my%20customer%20account%20password.%0A%0ARegistered%20email%20or%20mobile%20number%3A%20">Forgot Password? Contact us</a>{message&&<p className="account-error" role="alert">{message}</p>}<button className="button primary" disabled={busy}>{busy?"Signing in…":"Customer Login"}</button></form>}
