// Keep the completed reset implementation dormant until transactional email is configured.
// Toggle only as part of the separately approved password-reset release.
export const customerPasswordResetEnabled = false;
