"use client";
import { useState } from "react";
export default function UserAccess({userId,currentRoleId,status,roles}:{userId:string;currentRoleId:string;status:string;roles:{id:string;name:string}[]}){const [roleId,setRoleId]=useState(currentRoleId);const [state,setState]=useState(status);const [message,setMessage]=useState("");async function save(){const r=await fetch(`/api/admin/users/${userId}`,{method:"PUT",headers:{"content-type":"application/json"},body:JSON.stringify({roleId,status:state})});const d=await r.json() as {error?:string};setMessage(r.ok?"Saved.":d.error??"Unable to save.")}return <div className="admin-access-controls"><select aria-label="Assigned role" value={roleId} onChange={e=>setRoleId(e.target.value)}>{roles.map(r=><option key={r.id} value={r.id}>{r.name}</option>)}</select><select aria-label="Account status" value={state} onChange={e=>setState(e.target.value)}><option value="active">Active</option><option value="disabled">Disabled</option></select><button type="button" onClick={save}>Save</button>{message&&<small role="status">{message}</small>}</div>}
