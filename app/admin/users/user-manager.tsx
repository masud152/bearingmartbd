"use client";
import { useState,type FormEvent } from "react";
export default function UserManager({roles}:{roles:{id:string;name:string}[]}){const [message,setMessage]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch("/api/admin/users",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(f))});const d=await r.json() as {error?:string};if(!r.ok){setMessage(d.error??"Unable to add user.");return}location.reload()}return <form className="admin-inline-form" onSubmit={submit}><h2>Add administrator</h2><label>Email address<input type="email" name="email" required/></label><label>Display name<input name="displayName"/></label><label>Role<select name="roleId" required>{roles.map(r=><option value={r.id} key={r.id}>{r.name}</option>)}</select></label>{message&&<p role="alert">{message}</p>}<button className="admin-primary">Add user</button></form>}
