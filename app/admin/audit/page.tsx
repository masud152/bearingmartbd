import { requireAdmin } from "../_lib/auth";import { listAudit } from "../_lib/data";
export default async function Audit(){await requireAdmin("audit.view","/admin/audit");const events=await listAudit();return <div className="admin-page"><div className="admin-heading"><div><p className="admin-kicker">GOVERNANCE</p><h1>Audit log</h1><p>Recent catalogue, access and configuration activity.</p></div></div><div className="admin-table-wrap"><table className="admin-table"><caption>{events.length} recent event{events.length===1?"":"s"}</caption><thead><tr><th>Time</th><th>Actor</th><th>Action</th><th>Resource</th><th>Details</th></tr></thead><tbody>{events.map(e=><tr key={e.id}><td>{new Date(e.createdAt).toLocaleString()}</td><td>{e.actorEmail}</td><td><code>{e.action}</code></td><td>{e.resourceType}{e.resourceId?` · ${e.resourceId}`:""}</td><td>{e.details??"—"}</td></tr>)}</tbody></table></div></div>}
