import { ballBearingProducts } from "../../../ball-bearing-products";
export default function BallBearingCatalog() { return <main className="catalog-page"><header className="nav"><a className="brand" href="/">Bearing <span>Mart BD</span></a><a className="nav-call" href="https://wa.me/8801914528336">WhatsApp Now</a></header><section className="catalog-hero"><p className="eyebrow">BALL BEARINGS CATALOGUE</p><h1>Find your bearing.</h1><p>Dimensions and commercial availability are confirmed on enquiry.</p></section><section className="catalog-grid">{ballBearingProducts.map(([number,type,bore,outer,width]) => <a href={`/products/ball-bearings/catalog/${number}`} className="catalog-card" key={number}><span>{type}</span><h2>{number}</h2><p>NSK · {bore} × {outer} × {width} mm</p><strong>Price on enquiry →</strong></a>)}</section></main> }
