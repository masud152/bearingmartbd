import { getManagedBallBearingProducts } from "../../../managed-product-data";
import { productImageUrl } from "../../../bearing-product-images";
export default async function BallBearingCatalog() { const products=await getManagedBallBearingProducts(); return <main className="catalog-page"><header className="nav"><a className="brand" href="/">Bearing <span>Mart BD</span></a><a className="nav-call" href="https://wa.me/8801914528336">WhatsApp Now</a></header><section className="catalog-hero"><p className="eyebrow">BALL BEARINGS CATALOGUE</p><h1>Find your bearing.</h1><p>Dimensions and commercial availability are confirmed on enquiry.</p></section><section className="catalog-grid">{products.map(([number,type,bore,outer,width,imageKey]) => <a href={`/products/ball-bearings/catalog/${number}`} className="catalog-card" key={number}><img className="catalog-card-image" src={productImageUrl(number,imageKey)} alt={`NSK ${number} ${type}`} loading="lazy"/><span>{type}</span><h2>{number}</h2><p>NSK · {bore} × {outer} × {width} mm</p><strong>Price on enquiry →</strong></a>)}</section></main> }
